{
  "name": "finance-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first crop-farm financial planner (Lit + Dexie): cost tracking, break-even, seasonal pricing",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dexie": "^4.0.10",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
